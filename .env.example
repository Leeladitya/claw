# Claw — Environment Variables
# Copy this file to .env and fill in your values:
#   cp .env.example .env

# ── Required ────────────────────────────────────────────────────
# Your Anthropic API key (get one at https://console.anthropic.com)
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# ── Optional (defaults shown) ───────────────────────────────────
# Claude model to use for inference
CLAW_MODEL=claude-sonnet-4-5-20250514

# Policy pack to apply: standard | strict | research | finance | healthcare | community
CLAW_POLICY_PACK=standard

# Comma-separated API keys for authenticating requests to the Claw server (leave empty to disable auth)
CLAW_API_KEYS=

# Comma-separated allowed CORS origins
CLAW_CORS_ORIGINS=http://localhost:8787,moz-extension://*
